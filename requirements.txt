# Core runtime dependencies — sqc quantum sensing simulation platform.
#
# The authoritative dependency spec (with optional extras) lives in
# pyproject.toml. Recommended install:
#     pip install -e .            # core (this file)
#     pip install -e ".[demo]"    # + Gradio web demo (web_demo_v2.py)
#     pip install -e ".[test]"    # + pytest tooling
#
# This file mirrors the CORE set for `pip install -r requirements.txt`.
# Web-demo-only deps live in requirements_demo.txt.

# Core quantum simulation library
qutip>=5.0,<6.0

# Scientific computing and numerical libraries
numpy>=1.24,<3.0
scipy>=1.11,<2.0

# Data visualization
matplotlib>=3.7,<4.0

# Testing (dev) — also available via `pip install -e ".[test]"`
pytest>=7.4,<9.0
pytest-xdist>=3.5,<4.0
pytest-cov>=4.1,<6.0

# Optional (not installed by default):
#   Web demo:  see requirements_demo.txt  (gradio>=4.0,<5.0)
#   Jupyter:   jupyter, notebook
#   Progress:  tqdm
#   Parallel:  joblib
