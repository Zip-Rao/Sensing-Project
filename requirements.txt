# Requirements for superconducting qubit quantum sensing simulation platform

# Core quantum simulation and scientific computing
qutip>=5.0,<6.0
numpy>=1.24,<3.0
scipy>=1.11,<2.0
matplotlib>=3.7,<4.0

# Data analysis and optional optimization helpers used by notebooks/demos
pandas>=1.3.0
scikit-learn>=1.0
joblib>=1.1

# Web demo.
# Keep Gradio 3.x for now because the current web_demo.py is validated
# against gradio 3.24.1 in qutip-env. Upgrade to 4.x only after a demo check.
gradio>=3.24,<4.0

# Phase 0 test infrastructure
pytest>=7.4,<9.0
pytest-xdist>=3.5,<4.0
pytest-cov>=4.1,<6.0
