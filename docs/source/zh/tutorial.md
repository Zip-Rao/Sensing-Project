# 完整示例

这份端到端 notebook 完整运行{doc}`范例 <examples/index>`中的三条管道:波形重建、
频率标定、预失真。内容包括构建 qubit、施加磁通信号、用 QuTiP 仿真,以及重建、标定与
补偿,并给出对应的数值与图。它是范例的可运行版本:范例给出最短的调用配方,本页给出
带输出与中间结果的完整流程。输出已预先渲染,安装 `[demo]` extra 后也可在本地运行。

```{toctree}
:maxdepth: 1

传感仿真 notebook <Simulation_sqc>
```
