# Full Example

This end-to-end notebook walks through the `sqc` API on a full sensing example:
building a qubit, applying a flux signal, simulating with QuTiP, and
reconstructing the waveform. Its outputs are pre-rendered — you can also run it
locally after installing the `[demo]` extra.

```{note}
The notebook is currently authored in Chinese; an English walk-through is
planned. The code and outputs are language-neutral.
```

```{toctree}
:maxdepth: 1

Sensing simulation notebook <Simulation_sqc>
```
