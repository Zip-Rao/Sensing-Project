# Hardware Layer

```{note}
Content coming soon. Control-line / electronics layer: `ControlLine`,
`DistortionModel` and subclasses, `TransferMatrix` (extension point:
`DistortionModel`, `ReadoutModel` ABCs).
```
