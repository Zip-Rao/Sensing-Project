# Simulation Layer

```{note}
Content coming soon. QuTiP invocation layer: `HamiltonianBuilder`,
`MesolveRunner`, `SlidingMeasurementRunner`, `ExperimentResult`, noise
generation (extension point: `RunnerBase`).
```
