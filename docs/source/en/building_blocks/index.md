# Building Blocks

The platform is an eight-layer cQED stack. Each layer provides a set of objects
and an extension interface (an abstract base class). This section introduces
each layer: what it provides, its interface contract, and how to use it on its
own.

```{toctree}
:maxdepth: 1

devices
hardware
control
simulation
experiments
reconstruction
calibration
workflows
```
