# Python bytecode cache
__pycache__/
*.py[cod]
*.pyo

# Virtual environments
venv/
.venv/
env/

# IDE files
.vscode/
.idea/

# Jupyter
.ipynb_checkpoints/

# Distribution
dist/
build/
*.egg-info/

# Documentation site build output (Sphinx); source under docs/source/ is tracked
docs/build/
docs/source/**/generated/
# Tutorial notebook is copied into each language tree at build time from the
# repo-root single source of truth (see docs/Makefile); ignore the copies.
docs/source/en/Simulation_sqc.ipynb
docs/source/zh/Simulation_sqc.ipynb

# OS files
.DS_Store
Thumbs.db

# Local config (keep template)
.env

# ---------------------------------------------------------------------------
# Release hygiene (2026-07-17): keep only the core platform on GitHub.
# The folders below are kept LOCALLY but shielded from git (research outputs,
# design/theory notes, the paper, dev notebooks, archived loose files).
# Each has a local README explaining its contents. To re-track, remove its
# line here and `git add`.
# ---------------------------------------------------------------------------

# Research outputs / scratch (local only)
result/
output/
cell/
kernel/
note/
appendix/

# Design / theory / refactor-process notes (local only; core docs link into it)
idea/

# LaTeX paper (local only; edited locally)
report/

# Relocated from repo root (see each folder's README)
notebooks/
archive/

# Development-only, not part of the distributed core platform (local only):
#   legacy reference impl + mirror facade, test suite, agent/process docs.
src/
src_mirror/
tests/
pytest.ini
CLAUDE.md
AGENTS.md
RELEASE_TODO.md

# AI-assistant / editor tooling config (local only)
.claude/
.codex/
